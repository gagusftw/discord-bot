[tool.poetry]
name = "discord-bot"
version = "0.1.0"
description = ""
authors = ["Joseph Barshay"]
readme = "README.md"
packages = [{include = "discord_bot"}]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
